using UnityEngine;
using System.Collections;

public class LegsScript : MonoBehaviour {

	// Use this for initialization
	void Start () {
        //c.Log(this.GetComponent<Animation>().sta());
        animation.Play("BendKnee");
	}
	
	// Update is called once per frame
	void Update () {
	
	}
}
